DROP TABLE if exists graph;
CREATE TABLE graph(user text primary key,
                   fullname text,
                   invitee text,
                   tribe text,
                   career text,
                   id text);
